# Production deps (дублируются для удобства dev-окружения)
psutil>=6.1,<7

# Web framework / ORM / bot — подставляются из основного проекта,
# но нужны для локального запуска тестов.
fastapi>=0.111
aiosqlite>=0.20
sqlalchemy[asyncio]>=2.0
aiogram>=3.0
apscheduler>=3.10
sqladmin>=0.16

# Dev / test / lint / security
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0
httpx>=0.27
httpx2>=2.5
ruff>=0.5
bandit[toml]>=1.7
mypy>=1.10
